@page "/"

<PageTitle>Label Inspection Portal</PageTitle>

<div class="hero-panel">
    <div>
        <p class="eyebrow">Automated quality control</p>
        <h1>Label inspection portal</h1>
        <p class="lead">Upload manufacturing label images, send them to the ML service asynchronously, and review similarity and defect outcomes in one place.</p>
    </div>

    <div class="hero-actions">
        <AuthorizeView>
            <Authorized>
                <a href="/templates/upload" class="btn btn-outline-secondary btn-lg">Upload template</a>
                <a href="/images/upload" class="btn btn-primary btn-lg">Upload image</a>
                <a href="/images" class="btn btn-outline-secondary btn-lg">View my uploads</a>
            </Authorized>
            <NotAuthorized>
                <a href="/Account/Login" class="btn btn-primary btn-lg">Sign in</a>
                <a href="/Account/Register" class="btn btn-outline-secondary btn-lg">Create account</a>
            </NotAuthorized>
        </AuthorizeView>
    </div>
</div>

<div class="home-grid">
    <section class="info-card">
        <h2>Template-first flow</h2>
        <p>Upload a reference template with a friendly name and default tolerance, then select it when you upload an inspection image.</p>
    </section>
    <section class="info-card">
        <h2>Async processing</h2>
        <p>The ML service receives <code>image_url</code>, <code>template_url</code>, <code>callback_url</code>, and <code>image_id</code>, then posts similarity and defect results back through a shared-secret webhook.</p>
    </section>
    <section class="info-card">
        <h2>Outcome rule</h2>
        <p>Template tolerance seeds the image form, but users can override it. A tolerance of 20 means the image is accepted at 80% similarity or higher.</p>
    </section>
</div>
